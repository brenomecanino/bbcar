<Window x:Class="AutoOrcamento.Desktop.ClientDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Cliente" Height="420" Width="520" WindowStartupLocation="CenterOwner" ResizeMode="NoResize">
    <Grid Margin="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
            <ScrollViewer VerticalScrollBarVisibility="Auto">
            <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="120"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <Label Grid.Row="0" Grid.Column="0" Content="Nome:" VerticalAlignment="Center"/>
            <TextBox x:Name="NomeBox" Grid.Row="0" Grid.Column="1" Margin="6,4"/>

            <Label Grid.Row="1" Grid.Column="0" Content="Placa:" VerticalAlignment="Center"/>
            <TextBox x:Name="PlacaBox" Grid.Row="1" Grid.Column="1" Margin="6,4"/>

            <Label Grid.Row="2" Grid.Column="0" Content="Modelo:" VerticalAlignment="Center"/>
            <TextBox x:Name="ModeloBox" Grid.Row="2" Grid.Column="1" Margin="6,4"/>

            <Label Grid.Row="3" Grid.Column="0" Content="Ano:" VerticalAlignment="Center"/>
            <TextBox x:Name="AnoBox" Grid.Row="3" Grid.Column="1" Margin="6,4"/>

            <Label Grid.Row="4" Grid.Column="0" Content="Telefone:" VerticalAlignment="Center"/>
            <TextBox x:Name="TelefoneBox" Grid.Row="4" Grid.Column="1" Margin="6,4"/>

            <Label Grid.Row="5" Grid.Column="0" Content="CPF:" VerticalAlignment="Center"/>
            <TextBox x:Name="CpfBox" Grid.Row="5" Grid.Column="1" Margin="6,4"
                     PreviewTextInput="Cpf_PreviewTextInput" DataObject.Pasting="Cpf_Pasting" TextChanged="Cpf_TextChanged"/>

            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>
            </Grid>
            </ScrollViewer>

        <StackPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,12,0,0">
            <Button Content="Cancelar" Click="Cancel_Click" Width="96"/>
            <Button Content="OK" Click="Ok_Click" Width="96" Margin="8,0,0,0"/>
        </StackPanel>
    </Grid>
</Window>